name: ledge
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.8
  dartz: ^0.10.1
  http: ^1.6.0
  flutter_dotenv: ^6.0.1
  logger: ^2.7.0
  flutter_bloc: ^9.1.1
  bloc: ^9.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.5
  bloc_test: ^10.0.0

flutter:
  uses-material-design: true

  assets:
    - .env